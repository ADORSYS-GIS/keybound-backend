apiVersion: v1
kind: ConfigMap
metadata:
  name: user-storage-flows
  labels:
    {{- include "user-storage.labels" . | nindent 4 }}
data:
{{- range $path, $bytes := .Files.Glob "flows/*.yaml" }}
  {{ base $path }}: |-
{{ $.Files.Get $path | indent 4 }}
{{- end }}
